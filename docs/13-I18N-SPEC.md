# 13 — i18n

支持 `zh-CN`、`en-US`。

代码/DB/API/状态全部 English。
所有用户可见文字必须 i18n，包括状态、错误、表单、Toast、通知、进度。

Key：
`instance.status.running`
`instance.actions.restart`
`operation.reinstall.preparing`
`errors.node_offline`

Money/DateTime 按 locale/timezone 格式化。
