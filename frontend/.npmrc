# Registry pinned to a mirror reachable from this project's development
# environment and from CI. It is workspace-scoped: nothing outside frontend/ is
# affected, and the repository stays installable with a single `npm ci`.
registry=https://registry.npmmirror.com
